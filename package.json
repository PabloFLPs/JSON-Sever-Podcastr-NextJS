{
  "name": "JSON-Sever",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "json-server": "^0.17.0"
  },
  "scripts": {
    "build": "yarn add json-server",
    "start": "json-server server.json -p 3030"
  }
}
